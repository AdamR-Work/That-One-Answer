<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">

    <title>That One Answer</title>
    {{!-- <link rel="stylesheet" href="./stylesheets/style.css"> --}}
</head>

<body>
    <div class="container-lg">
        <header class="row">
            <h1 class="col-sm">
                <a href="/">That One Answer</a>
            </h1>

            <nav class="nav justify-content-begin">
                <div class="nav-item">
                    <a class="nav-link col-sm active" aria-current="page" href="/">Home</a>
                </div>
                <div class="nav-item">
                    <a class="nav-link active" href="/category">Category</a>
                </div>
                {{#if loggedIn}}
                <div class="nav-item">
                    <a class="nav-link active" href="/dashboard">Dashboard</a>
                </div>
                

                <div class="nav-item">
                    <a class="nav-link active" href="/create">Share Answer</a>
                </div>
                <div class="nav-item">
                    <button class="logout btn btn-no-style" id="logout">Logout</button>
                </div>
               
                {{else}}
                <div class="nav-item">
                    <a class="nav-link" href="/login">Login</a>
                </div>
                {{/if}}
                 
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>

        <br>
        <footer class="card w-50">
            <div class="card-header">Made with ❤ by Double Jeopardy</div>
            <div class="card-body">
                <blockquote class="blockquote mb-0">
                    <p id="quote">{{quote}}</p>
                    <footer class="blockquote-footer">Ron Swanson</footer>
                </blockquote>
            </div>
        </footer>

        <!-- Option 1: Bootstrap Bundle with Popper -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-b5kHyXgcpbZJO/tY9Ul7kGkf1S0CWuKcCD38l8YkeH8z8QjE0GmW1gYU5S9FOnJ0"
            crossorigin="anonymous"></script>


        {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>

        {{/if}}
    </div>
</body>

</html>
